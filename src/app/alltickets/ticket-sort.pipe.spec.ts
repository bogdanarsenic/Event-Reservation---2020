import { TicketSortPipe } from './ticket-sort.pipe';

describe('TicketSortPipe', () => {
  it('create an instance', () => {
    const pipe = new TicketSortPipe();
    expect(pipe).toBeTruthy();
  });
});
