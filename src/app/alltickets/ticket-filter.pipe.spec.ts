import { TicketFilterPipe } from './ticket-filter.pipe';

describe('TicketFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TicketFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
